# Installation
-------------------------------------------------------------------------------
All plugins will auto-install as long as the repo is cloned in the default
location. This is:

## Linux
---
- Clone repo in `~/.config`

## Windows
---
- Clone repo in `~/AppData/Local/`
